{% extends 'base.html.twig' %}

{% block title %}PanierLine{% endblock %}

{% block body %}
    <h1>PanierLine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panier_line.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ panier_line.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('panier_line_index') }}">back to list</a>

    <a href="{{ path('panier_line_edit', {'id': panier_line.id}) }}">edit</a>

    {{ include('panier_line/_delete_form.html.twig') }}
{% endblock %}
